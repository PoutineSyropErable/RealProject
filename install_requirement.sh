#!/bin/bash
echo "You don't execute this, you do the command in your terminal once conda is activated"
echo 
echo "conda env export > environment.yml"
exit 0
# do the following command
# Gave it execute permission so its colored by ls
conda env create -f environment.yml

